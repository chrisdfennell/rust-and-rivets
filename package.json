{
  "name": "rust-and-rivets",
  "version": "0.1.0",
  "description": "Dieselpunk roguelike deckbuilder — pilot a salvaged mech through the wasteland",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.1"
  }
}
